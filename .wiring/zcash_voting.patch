diff --git a/Cargo.toml b/Cargo.toml
index 97c7c920..00000000 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -5,3 +5,16 @@ members = [
     "vote-commitment-tree-client",
 ]
 resolver = "2"
+
+# Local overrides: sibling repos as relative paths.
+# valar-pczt is published from valargroup/librustzcash adam/crates-io-hygiene;
+# the local librustzcash main has it as `pczt`, so we rename via `package`.
+[patch.crates-io]
+valar-orchard    = { path = "../voting-circuits/orchard" }
+voting-circuits  = { path = "../voting-circuits/voting-circuits" }
+imt-tree         = { path = "../vote-nullifier-pir/imt-tree" }
+pir-client       = { path = "../vote-nullifier-pir/pir/client" }
+valar-pczt       = { path = "../librustzcash/pczt", package = "pczt" }
+zcash_keys       = { path = "../librustzcash/zcash_keys" }
+zcash_primitives = { path = "../librustzcash/zcash_primitives" }
+zcash_protocol   = { path = "../librustzcash/components/zcash_protocol" }
