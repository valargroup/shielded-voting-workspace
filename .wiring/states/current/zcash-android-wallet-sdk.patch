diff --git a/backend-lib/Cargo.toml b/backend-lib/Cargo.toml
index d573c6ef..4efec8a2 100644
--- a/backend-lib/Cargo.toml
+++ b/backend-lib/Cargo.toml
@@ -37,7 +37,7 @@ eip681 = "0.1.0"
 
 # Shielded voting (valargroup/zcash_voting)
 # Pinned to main until the next crates.io release includes the Android/iOS parity APIs.
-zcash_voting = { git = "https://github.com/valargroup/zcash_voting.git", rev = "e98eb12aa629a9eb8f0c8246f1f4d60be8166367", default-features = false, features = [
+zcash_voting = { path = "../../zcash_voting/zcash_voting", default-features = false, features = [
     "client-pir",
     "client-tree-sync",
 ] }
@@ -93,7 +93,13 @@ xz2 = { version = "0.1", features = ["static"] }
 component = "0.1.1"
 rust-analyzer = "0.0.1"
 
+[patch.crates-io]
+orchard = { path = "../../orchard" }
+voting-circuits = { path = "../../voting-circuits/voting-circuits" }
+imt-tree = { path = "../../vote-nullifier-pir/imt-tree" }
+pir-client = { path = "../../vote-nullifier-pir/pir/client" }
+
 [lib]
 name = "zcashwalletsdk"
 path = "src/main/rust/lib.rs"
 crate-type = ["staticlib", "cdylib"]
