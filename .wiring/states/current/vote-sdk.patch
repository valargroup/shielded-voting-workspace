diff --git a/circuits/Cargo.toml b/circuits/Cargo.toml
--- a/circuits/Cargo.toml
+++ b/circuits/Cargo.toml
@@ -13,10 +13,10 @@ pasta_curves = "0.5"
 group = "0.13"
 rand_core = { version = "0.6", features = ["getrandom"] }
 reddsa = { version = "0.5", default-features = false }
-vote-commitment-tree = "0.1.1"
-orchard = { version = "=0.13.1", features = ["unstable-voting-circuits"] }
-voting-circuits = { version = "0.3.1", features = ["share-reveal"] }
-imt-tree = { git = "https://github.com/valargroup/vote-nullifier-pir.git" }
+vote-commitment-tree = { path = "../../zcash_voting/vote-commitment-tree" }
+orchard = { path = "../../voting-circuits/orchard" }
+voting-circuits = { path = "../../voting-circuits/voting-circuits", features = ["share-reveal"] }
+imt-tree = { path = "../../vote-nullifier-pir/imt-tree" }
 incrementalmerkletree = "0.8"
 hex = "0.4"
 halo2_gadgets = "0.4"
diff --git a/e2e-tests/Cargo.toml b/e2e-tests/Cargo.toml
--- a/e2e-tests/Cargo.toml
+++ b/e2e-tests/Cargo.toml
@@ -8,11 +8,11 @@ description = "Rust E2E API tests for Shielded-Vote voting flow (replaces TypeSc
 # Same-repo path dep (circuits/ is a sibling directory)
 shielded-vote-circuits = { path = "../circuits" }
 
-orchard = { version = "=0.13.1", features = ["unstable-voting-circuits"] }
-voting-circuits = { version = "0.3.1", features = ["share-reveal"] }
-vote-commitment-tree = "0.1.1"
-vote-commitment-tree-client = "0.1.1"
-zcash_voting = "0.1.1"
+orchard = { path = "../../orchard" }
+voting-circuits = { path = "../../voting-circuits/voting-circuits" }
+vote-commitment-tree = { path = "../../zcash_voting/vote-commitment-tree" }
+vote-commitment-tree-client = { path = "../../zcash_voting/vote-commitment-tree-client" }
+zcash_voting = { path = "../../zcash_voting/zcash_voting" }
 
 # HTTP and JSON
 reqwest = { version = "0.12", features = ["blocking", "json"] }
