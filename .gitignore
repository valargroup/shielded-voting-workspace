# Child repos — each is a standalone clone, not managed by the umbrella.
zcash_voting/
librustzcash/
orchard/
vote-nullifier-pir/
vote-sdk/
voting-circuits/
zcash-android-wallet-sdk/
zcash-swift-wallet-sdk/
zebra/
shielded-vote-book/
zodl-android/
zodl-ios/
keystone3-firmware/
token-holder-voting-config/
vote-infrastructure/
zips/
ypir/
spiral-rs/
cosmos-sdk-v0.53.5/

# Chain data (was ~/.svoted, now local for sandboxing)
.svoted/

# Service logs
svoted.log
nf-serve.log
ui.log
android-emulator.log
auth.log

# PID files
.pids/

# IDE / OS
.DS_Store
*.swp

# Local env (secrets, machine-specific)
.env

# Wiring: active state is per-checkout, not committed
.wiring/current-state
