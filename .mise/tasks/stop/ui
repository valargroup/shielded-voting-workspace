#!/usr/bin/env bash
#MISE description="Stop only admin UI (leave svoted + nf-server running)"
#MISE quiet=true

if pkill -f 'node.*vote-shielded-vote-generator' 2>/dev/null; then
    printf '\033[32mStopped admin UI\033[0m\n'
else
    echo "admin UI is not running."
fi
